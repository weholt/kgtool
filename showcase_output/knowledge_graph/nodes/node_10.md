# 4.1 Common Principles

**Tags:** frontend

**Keywords:** limiting client, logs outbox, limiting, independently deployable, independently, immutable event, event logs, driven migrations

**Keyphrases:** Immutable event logs, service independently deployable, CD-driven migrations, Public contracts, Immutable event, Outbox pattern, Rate limiting, independently deployable, versioned OpenAPI, event logs

---

- Each service independently deployable  
- Public contracts via versioned OpenAPI  
- Immutable event logs  
- Outbox pattern for reliable delivery  
- CI/CD-driven migrations  
- Rate limiting per client and tenant