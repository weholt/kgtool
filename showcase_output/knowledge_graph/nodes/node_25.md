# 7.2 Key Management

**Tags:** data

**Keywords:** keys stored, keys, hsm, hsm backed, bearer, uses, aggregates, analytics

**Keyphrases:** Keys stored, HSM-backed vault, stored in HSM-backed, Keys, vault, stored, HSM-backed

---

Keys stored in HSM-backed vault.